<h1>Hello <%= params[:user].email %></h1>
<h3>Reset your password</h3>

<%# use url for external site %>
The link will automatically expire in 15 minutes.
<%= password_reset_edit_url(token: @token) %>
